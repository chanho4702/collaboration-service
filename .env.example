HOST=0.0.0.0
PORT=19150
# 도커 Redis의 호스트 게시 포트는 16379다(compose REDIS_HOST_PORT). 127.0.0.1:6379는 호스트의 Windows Redis 3.2가 답한다 — 쓰지 않는다.
REDIS_URL=redis://localhost:16379/1
DATABASE_URL=postgresql://keycloak:keycloak@localhost:5433/wikidb
MAX_DOCUMENT_BYTES=10485760
SHUTDOWN_TIMEOUT_MS=10000
